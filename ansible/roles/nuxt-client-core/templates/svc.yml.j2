apiVersion: v1
kind: Service
metadata:
  name: nuxtclient-svc
  namespace: {{ NAMESPACE }}
  labels:
    app: nuxtclient
spec:
  type: ClusterIP
  ports:
    - port: {{ NUXTCLIENT_PORT }}
      targetPort: 4000
      protocol: TCP
      name: nuxtclient
  selector:
    app: nuxtclient
