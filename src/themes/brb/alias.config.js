const config = {
	replacements: [
		/**
		 * Enter the imports to be replaced below.
		 * Note: only imports with "@/" path alias are supported.
		 */
		"@/vuetify.options",
		"@/theme.config",
		"@/assets/img/logo/logo-image-mono.svg",
		"@/assets/img/logo/logo-transparent-mono-48-48.svg",
		"@/components/templates/impressum",
	],
};

module.exports = config;
