import serviceTemplate from "@utils/service-template";
const base = serviceTemplate("courses");

// base.state = () => {
// 	let defaultState = base.state();
// 	defaultState.
// }

const module = {
	...base,
};

export default module;
