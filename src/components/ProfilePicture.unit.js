import { shallowMount } from "@vue/test-utils";
import ProfilePicture from "./ProfilePicture";

describe("@components/ProfilePicture", () => {
	it("exports a valid component", () => {
		expect(ProfilePicture).toBeAComponent();
	});
	//TODO
});
