import { shallowMount } from "@vue/test-utils";
import StepProgress from "./StepProgress";

describe("@components/StepProgress", () => {
	it("exports a valid component", () => {
		expect(StepProgress).toBeAComponent();
	});
	//TODO
});
