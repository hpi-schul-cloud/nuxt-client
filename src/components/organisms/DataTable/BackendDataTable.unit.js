import BackendDataTable from "./BackendDataTable";

describe("@components/BackendDataTable", () => {
	it(...isValidComponent(BackendDataTable));

	it.todo("should pass slots prefixed with dataRow to tableRow component");
});
