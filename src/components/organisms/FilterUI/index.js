import FilterUI from "./Filter";
import filterInputs from "./inputs";
import filterLayouts from "./layouts";
import queryParser from "./parser";

export const inputs = filterInputs;
export const layouts = filterLayouts;
export const parser = queryParser;

export default FilterUI;
