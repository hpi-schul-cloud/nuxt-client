import { storiesOf } from "@storybook/vue";

storiesOf("4 Base UI Components", module).add("4 Base UI Components", () => ({
	template: `
	<div>
	<h1>Base UI Components</h1>
	<p>The Base UI Components include very basic interface molecules, such as input fields, date-pickers, basic dialogues and modals.</p>
	</div>
	`,
}));
